class Solution {
  public:
    int findFrequency(vector<int> arr, int x) {
        map<int,int>cnt;
        for(int i=0;i<arr.size();i++){
                cnt[arr[i]]++;
        }
        
        return cnt[x];
    }
};
